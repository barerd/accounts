require 'rubygems'
require 'cucumber/rake/task'
 
Cucumber::Rake::Task.new(:signon) do |t|
  t.cucumber_opts = "--format pretty --tags @signon-form"
end
 
Cucumber::Rake::Task.new(:features) do |t|
  t.cucumber_opts = "--format pretty"
end
